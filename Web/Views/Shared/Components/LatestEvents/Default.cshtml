@model dynamic[]

@foreach (var evt in Model)
{
    <div class="widget-post">
        <div class="post-content">
            <h6 class="post-title"><a href="javascript:void(0);">@evt.Title</a></h6>
            <span class="post-date">@evt.Date</span>
        </div>
    </div>
}
