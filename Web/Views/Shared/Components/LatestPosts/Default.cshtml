@model dynamic[]

@foreach (var post in Model)
{
    <div class="widget-post">
        <div class="post-content">
            <h6 class="post-title"><a href="javascript:void(0);">@post.Title</a></h6>
            <span class="post-date">@post.Date</span>
        </div>
    </div>
}
